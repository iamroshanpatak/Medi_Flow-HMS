'use client';

// This component is not currently used but kept for future debugging
// It had hydration issues due to localStorage being different on server vs client
// To use it in the future, dynamically import it with { ssr: false }

export default function LanguageDebug() {
  return null;
}
